# npm 配置文件
# 用于加速依赖安装（可选）

# 使用淘宝镜像（国内加速）
# registry=https://registry.npmmirror.com

# 使用 npm 官方源（默认）
registry=https://registry.npmjs.org

# 自动解决依赖冲突（如果遇到 peer dependency 警告）
legacy-peer-deps=true

# 启用严格的 SSL 检查
strict-ssl=true

# 保存精确版本号
save-exact=false

# 安装时的日志级别
loglevel=warn
